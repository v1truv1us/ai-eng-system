# Gap Analysis: Missing Skills, Commands, and Agents from External Repositories

## Objective
Analyze what's missing from the current ai-eng-system compared to:
- **addyosmani/agent-skills** (27.1k stars, 21 skills, 3 agents, 7 commands)
- **affaan-m/everything-claude-code** (140k stars, 182 skills, 48 agents, 68 commands)

And create a prioritized plan to integrate the most valuable additions.

## Current State (ai-eng-system)
- **38 skills** across `.claude/skills/`, `.opencode/skill/`, `skills/`
- **60 commands** across `.claude/commands/`, `.opencode/command/ai-eng/`
- **38 agents** across `.opencode/agent/ai-eng/`

## Gap Analysis Summary

### High-Priority Additions (from addyosmani/agent-skills)

These are workflow-quality skills that encode Google engineering practices and are NOT in ai-eng-system:

| # | Item | Type | Why Important |
|---|------|------|---------------|
| 1 | **Anti-rationalization tables** | Skill enhancement | Every skill needs "common excuses + counter-arguments" tables to prevent agents from skipping steps |
| 2 | **Orchestration patterns catalog** | Reference | `references/orchestration-patterns.md` - defines 5 endorsed + 4 anti-patterns for agent orchestration |
| 3 | **Source-driven development** | Skill | Already exists in ai-eng-system but may need anti-rationalization tables added |
| 4 | **Reference checklists** | Reference files | `testing-patterns.md`, `security-checklist.md`, `performance-checklist.md`, `accessibility-checklist.md` - loaded on-demand by skills |
| 5 | **Session lifecycle hooks** | Hooks | `hooks.json`, `session-start.sh`, `sdd-cache-pre.sh`, etc. for automated session management |
| 6 | **Multi-platform setup guides** | Documentation | Setup guides for Cursor, Gemini CLI, Windsurf, GitHub Copilot, Kiro (currently only Claude Code + OpenCode) |
| 7 | **`using-agent-skills` meta-skill** | Skill | Decision tree for task-to-skill mapping, core operating behaviors |

### High-Priority Additions (from everything-claude-code)

These fill real capability gaps in ai-eng-system:

| # | Item | Type | Why Important |
|---|------|------|---------------|
| 8 | **Continuous learning system** | Skill | Instinct-based learning with confidence scoring (v2) - system learns from sessions |
| 9 | **Language-specific reviewers** | Agents | TypeScript, Python, Go, Java, Rust, C++, Kotlin, C# specific code reviewers |
| 10 | **Rules system** | Rules | Always-follow guidelines per language ecosystem (TypeScript, Python, Go, Swift, etc.) |
| 11 | **Hook system with runtime controls** | Hooks | PreToolUse, PostToolUse, Stop, SessionStart hooks with env var gating |
| 12 | **MCP server configurations** | Config | Pre-configured MCP servers for GitHub, Supabase, Vercel, Railway |
| 13 | **Context files** | Contexts | Dynamic system prompt injection (`dev.md`, `review.md`, `research.md`) |
| 14 | **AgentShield security scanner** | Tool | 1282 tests, 98% coverage, 102 static analysis rules |
| 15 | **Verification loop skill** | Skill | Continuous verification methodology |
| 16 | **Eval harness skill** | Skill | Agent evaluation framework |
| 17 | **Context budget management** | Skill | Context window optimization |
| 18 | **Silent failure hunter agent** | Agent | Detects silent failures in agent workflows |

### Medium-Priority Additions

| # | Item | Type | Source |
|---|------|------|--------|
| 19 | PRP (Product Requirements Pipeline) commands | Commands | ECC |
| 20 | Open source contribution agents | Agents | ECC |
| 21 | GAN workflow agents | Agents | ECC |
| 22 | Media creation skills (manim, remotion) | Skills | ECC |
| 23 | Content/business skills | Skills | ECC |
| 24 | Agent introspection debugging | Skill | ECC |
| 25 | Agent payment protocol (X402) | Skill | ECC |
| 26 | Liquid Glass design (iOS 26) | Skill | ECC |
| 27 | Foundation models on device | Skill | ECC |

### Low-Priority / Nice-to-Have

| # | Item | Type | Source |
|---|------|------|--------|
| 28 | Dashboard GUI | Tool | ECC |
| 29 | Rust control-plane (ECC 2.0) | Tool | ECC |
| 30 | Multi-agent orchestration commands | Commands | ECC |
| 31 | Hookify commands | Commands | ECC |
| 32 | Chinese language rules | Rules | ECC |

## Recommended Implementation Plan

**User Decision**: All 4 phases, hybrid approach (import core concepts, rewrite to match ai-eng-system conventions)

### Phase 1: Core Workflow Enhancements
**Focus: Anti-rationalization tables + reference checklists + orchestration patterns**

1. Add anti-rationalization tables to all 38 existing skills
2. Create `references/` directory with:
   - `testing-patterns.md`
   - `security-checklist.md`
   - `performance-checklist.md`
   - `accessibility-checklist.md`
   - `orchestration-patterns.md`
3. Create `using-agent-skills` meta-skill
4. Add session lifecycle hooks (`hooks/` directory)

**Files to modify:** All `skills/*/SKILL.md` files (append anti-rationalization tables)
**Files to create:** `references/*.md`, `hooks/hooks.json`, `hooks/*.sh`

### Phase 2: Agent & Rules Expansion (Week 2)
**Focus: Language-specific agents + rules system + contexts**

5. Create language-specific reviewer agents:
   - `typescript-reviewer`, `python-reviewer`, `go-reviewer`, `java-reviewer`, `rust-reviewer`, `cpp-reviewer`, `kotlin-reviewer`, `csharp-reviewer`
6. Create `rules/` directory with language-specific rule packs
7. Create `contexts/` directory with `dev.md`, `review.md`, `research.md`
8. Add `silent-failure-hunter` agent
9. Add `loop-operator` agent

**Files to create:** `.opencode/agent/ai-eng/quality/*.md` (8 new agents), `rules/**/*.md`, `contexts/*.md`

### Phase 3: Advanced Capabilities (Week 3)
**Focus: Continuous learning + verification + security**

10. Create `continuous-learning-v2` skill
11. Create `verification-loop` skill
12. Create `eval-harness` skill
13. Create `context-budget` skill
14. Integrate AgentShield security scanner
15. Create MCP server configurations

**Files to create:** `skills/continuous-learning-v2/SKILL.md`, `skills/verification-loop/SKILL.md`, `skills/eval-harness/SKILL.md`, `skills/context-budget/SKILL.md`, `mcp-configs/mcp-servers.json`

### Phase 4: Platform Support & Documentation (Week 4)
**Focus: Multi-platform setup + missing commands**

16. Create setup guides for Cursor, Gemini CLI, Windsurf, GitHub Copilot, Kiro
17. Add PRP commands (`prp-prd`, `prp-plan`, `prp-implement`, `prp-commit`, `prp-pr`)
18. Add loop commands (`loop-start`, `loop-status`)
19. Add harness commands (`harness-audit`, `model-route`)
20. Update CLAUDE.md and AGENTS.md with new inventory

**Files to create:** `docs/cursor-setup.md`, `docs/gemini-cli-setup.md`, etc., `.claude/commands/prp-*.md`, `.claude/commands/loop-*.md`

## Verification

After implementation:
1. Run `bun run build` to ensure all new components compile
2. Run `bun run validate` to verify structure
3. Test a few new skills/commands in practice
4. Update AGENTS.md inventory tables
5. Verify sync between `.claude/` and `.opencode/` directories

## Key Files to Reference

- `CLAUDE.md` - Project philosophy
- `AGENTS.md` - Current agent registry
- `skills/` - Source skill definitions
- `.claude/commands/` - Claude Code commands
- `.opencode/agent/ai-eng/` - OpenCode agents
- `.opencode/command/ai-eng/` - OpenCode commands
- `scripts/install.js` - Build/sync process

## Constraints

- Maintain existing naming conventions
- Follow the three-platform sync model (source → `.claude/` → `.opencode/`)
- Keep skills token-conscious (progressive disclosure)
- Maintain YAML frontmatter format for commands and agents
- Do not break existing functionality
