---
name: spec
description: Start spec-driven development - write a structured specification before writing code
agent: plan
version: 1.0.0
---

# Spec (Lifecycle Alias)

This is a lifecycle alias for `/ai-eng/specify`. See `content/commands/specify.md` for the full command.

Load the `spec-driven-development` skill. Begin by understanding what needs to be built, then generate a structured specification covering objectives, interface contracts, project structure, code style, testing strategy, and boundaries.

Save the spec as `specs/{feature}/spec.md` and confirm before proceeding to `/plan`.

$ARGUMENTS
