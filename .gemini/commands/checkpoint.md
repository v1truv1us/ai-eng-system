---
name: checkpoint
description: Save a checkpoint of the current session state
agent: build
version: 1.0.0
---

# Checkpoint Command

Save the current session state so it can be resumed later.

```
/checkpoint
/checkpoint --label "auth-middleware-complete"
```

See also: `/resume-session`, `/sessions`

$ARGUMENTS
