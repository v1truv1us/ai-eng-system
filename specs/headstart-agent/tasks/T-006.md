# T-006: Implement GitHub API Wrapper

**Depends On:** T-002
**Priority:** critical
**Estimated Time:** 40 min
**Complexity:** Medium

## Acceptance Criteria
- [ ] `src/github.ts` exports functions for branch creation, commit push, and PR creation
- [ ] Uses `@octokit/rest` with `config.GITHUB_TOKEN`
- [ ] `createBranch(base, head)` creates a new branch from latest `base`
- [ ] `createPullRequest(params)` creates PR with title, head, base, body, labels; returns PR URL
- [ ] Handles 404/403 errors from GitHub API gracefully with descriptive error messages
- [ ] Unit tests pass with mocked Octokit: branch creation succeeds, PR creation returns URL, API errors throw

## Files
- `src/github.ts` — Create Octokit wrapper
- `tests/unit/github.test.ts` — Create unit tests with mocked Octokit

## Spec Reference
- Section 2.4: GitHub PR Creation
- Section 6.1 #1: Always create a PR
- Section 6.3 #1: Never push to main
