# T-004: Define Shared Types

**Depends On:** T-001
**Priority:** high
**Estimated Time:** 15 min
**Complexity:** Low

## Acceptance Criteria
- [ ] `src/types.ts` exports `Task`, `TaskResult`, `JobStatus`, `Job`, and `AgentError` types
- [ ] `Task` interface has `readonly type: string` and `run(payload: unknown): Promise<TaskResult>`
- [ ] `TaskResult` has `success`, optional `branch`, optional `prUrl`, and `message`
- [ ] `JobStatus` is a union type: `'pending' | 'running' | 'completed' | 'failed'`
- [ ] `Job` interface matches SQLite schema from spec
- [ ] `AgentError` extends Error with a `code: string` field

## Files
- `src/types.ts` — Create all shared TypeScript interfaces and types

## Spec Reference
- Section 2.3: Internal Task Runner API
- Section 5.3 (implied): Types used across tests
