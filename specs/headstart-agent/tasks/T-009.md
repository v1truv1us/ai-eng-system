# T-009: Implement Newsletter Task

**Depends On:** T-005, T-006, T-007
**Priority:** critical
**Estimated Time:** 60 min
**Complexity:** High

## Acceptance Criteria
- [ ] `src/tasks/newsletter.ts` exports a `Task` implementation with `type = 'newsletter'`
- [ ] `run(payload)` parses `month` and `year` from payload
- [ ] Calls `ensureRepo()` to get fresh repo at `/repos/headstart`
- [ ] Copies PDF from temp path to `public/pdfs/{Month} Newsletter {Year}.pdf`
- [ ] Reads `src/content/calendar/_index.json`, prepends new entry to `newsletters` array
- [ ] Checks idempotency: if branch `agent/newsletter-{month}-{year}` already exists, returns `success=true` with skip message
- [ ] Creates branch `agent/newsletter-{month}-{year}`
- [ ] Commits changes with message `Add {Month} {Year} newsletter`
- [ ] Pushes branch
- [ ] Creates PR with title `Add {Month} {Year} newsletter`, labels `automated,newsletter`
- [ ] On failure: cleans up partial branch and uncommitted changes, throws error for retry
- [ ] Returns `TaskResult` with `prUrl` on success
- [ ] Unit tests pass: valid flow, idempotent skip, invalid month parsing, cleanup on failure

## Files
- `src/tasks/newsletter.ts` — Create newsletter task
- `src/tasks/index.ts` — Create task registry (maps type string to Task class)
- `tests/unit/tasks/newsletter.test.ts` — Create unit tests

## Spec Reference
- Section 4.1: Newsletter Ingestion (core capability)
- Section 2.4: PR Body Template (Newsletter)
- Section 6.1 #4: Idempotency check
- Section 6.1 #7: Leave repo clean on failure
- AC-1: Newsletter Webhook
- AC-4: Idempotent Newsletter Processing
