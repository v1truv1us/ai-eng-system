# T-007: Implement Repo Clone/Pull Helpers

**Depends On:** T-002, T-006
**Priority:** critical
**Estimated Time:** 35 min
**Complexity:** Medium

## Acceptance Criteria
- [ ] `src/repo.ts` exports: `ensureRepo()`, `createCommit(files, message, branch)`, `pushBranch(branch)`
- [ ] `ensureRepo()` clones repo to `/repos/headstart` if missing; pulls latest `main` if exists
- [ ] `createCommit()` stages specified files, commits with given message on given branch
- [ ] `pushBranch()` pushes branch to origin
- [ ] Uses simple-git library for git operations
- [ ] GitHub auth for clone/push uses `config.GITHUB_TOKEN` embedded in HTTPS URL
- [ ] Unit tests pass with mocked simple-git: clone, pull, commit, push behaviors

## Files
- `src/repo.ts` — Create git operations wrapper
- `tests/unit/repo.test.ts` — Create unit tests with mocked simple-git

## Spec Reference
- Section 3.3: Runtime Directory Layout (`/repos`)
- Section 6.1 #6: Always clone/pull fresh
- Section 6.1 #7: Always leave repo clean on failure
