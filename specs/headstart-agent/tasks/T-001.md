# T-001: Initialize Project Scaffold

**Depends On:** None
**Priority:** critical
**Estimated Time:** 20 min
**Complexity:** Low

## Acceptance Criteria
- [ ] `package.json` created with Bun runtime, Fastify, zod, pino, better-sqlite3, @octokit/rest, node-cron, @sentry/node, nodemailer dependencies
- [ ] `tsconfig.json` created with strict mode and path aliases (`@/*` → `src/*`)
- [ ] `.gitignore` includes `node_modules/`, `.env`, `data/`, `repos/`, `dist/`
- [ ] `.env.example` lists all 14 env vars from spec section 7
- [ ] `src/` directory structure created (empty files are OK)
- [ ] README.md has project description and local dev setup instructions

## Files
- `package.json` — Create with dependencies and scripts (`dev`, `build`, `start`, `test`, `typecheck`)
- `tsconfig.json` — Create with strict TypeScript config
- `.gitignore` — Create standard Node.js gitignore
- `.env.example` — Create from spec section 7
- `src/index.ts` — Create empty entry point
- `src/types.ts` — Create empty types file
- `README.md` — Create with setup instructions

## Spec Reference
- Section 3.1: Project Structure
- Section 4.1: Language & Runtime
- Section 7: Configuration
