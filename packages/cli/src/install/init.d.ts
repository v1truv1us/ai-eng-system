/**
 * Initialize ai-eng configuration file
 */
import type { InitFlags } from "../cli/flags";
/**
 * Initialize configuration file
 */
export declare function initConfig(flags: InitFlags): Promise<void>;
