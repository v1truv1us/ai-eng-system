---
name: ship
description: Deploy to production with confidence
agent: build
version: 1.0.0
---

# Ship (Lifecycle Alias)

This is a lifecycle alias for `/ai-eng/deploy`. See `content/commands/deploy.md` for the full command.

Load the `shipping-and-launch` and `ci-cd-and-automation` skills. Execute the pre-launch checklist, deploy through staged rollout, and monitor at each stage.

$ARGUMENTS
